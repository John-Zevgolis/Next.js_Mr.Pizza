import Loader from '../../../components/Loader';

export default function Loading() {
  return (
    <div className="custom-padding-top custom-padding-bottom min-h-screen flex justify-center items-center">
      <Loader />
    </div>
  );
}
